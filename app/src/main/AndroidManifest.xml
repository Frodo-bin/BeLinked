<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nearby.belinked">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeLinked"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="m">
        <activity
            android:name=".activities.Register3Activity"
            android:label=""
            android:launchMode="singleTask"
            android:theme="@style/App.NoActionBar"></activity>
        <activity
            android:name=".activities.Register2Activity"
            android:label=""
            android:launchMode="singleTask"
            android:theme="@style/App.NoActionBar" />
        <activity
            android:name=".activities.Register1Activity"
            android:label=""
            android:launchMode="singleTask"
            android:theme="@style/App.NoActionBar" />
        <activity android:name=".activities.BaseActivity" />
        <activity
            android:name=".activities.ResetPasswordActivity"
            android:label=""
            android:launchMode="singleTask"
            android:theme="@style/App.NoActionBar" />
        <activity
            android:name=".activities.LoginByPwActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Splash" />
        <activity
            android:name=".activities.SplashActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Splash" /> <!-- 全屏设置，必须加入 -->
        <meta-data
            android:name="android.notch_support"
            android:value="true" />
    </application>

</manifest>